package net.catacombsnatch.game.core.event.input;

public enum InputSource {
	/** All keyboard related inputs */
	KEYBOARD,
	
	/** All mouse related inputs (left, middle and right click, scrolling)*/
	MOUSE,
	
	/** All gamepad / joystick related inputs (axis, POV, buttons, ...) */
	CONTROLLER;
}
